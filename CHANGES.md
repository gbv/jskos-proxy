# 0.5.4

- Implement per-backend caching for backend requests #87
- Fix concept redirects for schemes with external canonical URIs
- Migrate server and backend tests to Vitest
- Add a Husky pre-commit hook for running tests

# 0.5.3

- Fix case of one single vocabulary from backend results in an error
- Add CORS headers
