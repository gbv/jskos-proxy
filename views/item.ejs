<% if(jskos) { %>
  <script type="application/ld+json" id="item">
    <%- JSON.stringify(jskos).replaceAll('<','\u003C') %>
  </script>
  <div id="app">
    <!-- TODO: minimal preview without client side JavaScript -->
    <%= jskos.prefLabel?.en %>
    uri: <a href="<%= uri %>"><%= uri %></a>
  </div>
<% } else { %>
  <p>
    <a href="<%= uri %>"><%= uri %></a> not found
  </p>
<% } %>
