TITLE=BARTOC
LOGO=bartoc-logo.svg

NAMESPACE=http://uri.gbv.de/terminology/
BACKEND=https://bartoc.org/api/

# Global default (used when no override is given)
REQUIRE_CONCEPTS=true

# Per-registry overrides: "<base>=<true|false>" pairs, comma-separated
REQUIRE_CONCEPTS_OVERRIDES=https://bartoc.org/api/=false